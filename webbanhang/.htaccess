RewriteEngine On

# Bỏ qua nếu là file hoặc thư mục thực sự tồn tại
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route tất cả request còn lại qua index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
